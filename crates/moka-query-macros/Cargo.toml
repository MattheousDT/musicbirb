[package]
name = "moka-query-macros"
version = "0.1.0"
edition = "2021"
description = "Procedural macros for moka-query"

[lib]
proc-macro = true

[features]
uniffi = []

[dependencies]
syn = { version = "2.0", features = ["full", "extra-traits"] }
quote = "1.0"
proc-macro2 = "1.0"
